# Week 4 step 3y: one isolated live Nebius observation

## User prompt

> Move to next.

## Bounded interpretation

- Continue step 3x's proposed next action: one separately network-approved
  invocation of the instrumented isolated Nebius smoke test.
- Preserve the existing uncommitted work. No automatic commit or push.
- Use the existing fixed synthetic Candidate, Supervisor evidence, and Research
  Fit assessment. Do not run search, extraction, OpenAI, or the full graph.
- Retain the structured-response, score, evidence-reference, and reconciliation
  checks; do not tune prompts/models or weaken validation to obtain a pass.
- Run formatting, lint, types, and non-live tests first. Check only credential
  presence via the settings loader; never display secret values or read artifacts.
- Request network approval for exactly one Nebius invocation. Keep SDK retries
  at zero, the one-call guard, and the at-most-60-second request timeout.
- Force tracing off and suppress raw pytest tracebacks/logs. Record only fixed
  diagnostic codes, review outcome enums, call count, and elapsed time.
- Stop after the result. No rerun, private capture, Mem0, persistence, shortlist
  approval, outreach, trace upload, or speculative implementation repair.
- Update README, runbook, triage, and build journal with exact observed results,
  limits of the evidence, and the next bounded action. Cost/tokens remain unknown
  unless measured. An isolated pass is not a full-pipeline or quality benchmark.

## Follow-up checkpoint authorization

> lets commit first before we proceed to next step

The user paused the subsequent dataset work and explicitly authorized committing
the completed steps 3v–3y as a checkpoint. Revalidate the offline suite and stage
only the reviewed diagnostics, tests, prompts, and result documentation. No live
calls, private artifacts, push, or 30-case dataset implementation in this step.
