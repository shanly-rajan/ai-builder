# Week 4 step 3aa — record the first expected-behavior approval

## User response

> yes ScholarPath should accept the claim and It should retain the source URL and exact supporting text.

## Context and scope

The response follows an explanation of case 1 in the first review batch:
`draft-evidence-heading-bound-research`. A fictional individual profile clearly
binds its research section to the named Supervisor. The proposed behavior accepts
that directly supported research claim without requiring the name in each sentence,
while preserving the source URL and exact supporting text.

Record this explicit approval in a documentation-only review ledger tied to the
existing draft version and digest. Preserve the exact response, review date, and
reviewer role without inventing another person's identity.

Approve only this expected behavior and its provenance requirement. Keep the
other 29 cases pending, the immutable draft snapshot unchanged, and the actual
heading-grounding regression open. This is not authorization to implement a fix,
approve every technical label field, run live services, freeze/upload the dataset,
commit, or push. Continue the remaining review conversationally.
