# Week 4 step 3u: one live observation after grounded-excerpt repairs

## User prompt

> lets move to next

## Bounded interpretation

- Continue step 3t's proposed next action: one separately network-approved live
  canary after the two privately reproduced excerpt failures were repaired offline.
- Preserve the existing worktree. No automatic commit or push.
- Keep the existing synthetic Candidate, public target, configured models,
  strict verification rules, timeout clamps, and nine-logical-call ceiling.
- Run the current non-live suite before the live observation; check credential
  presence only through settings loaders, never printing secret values.
- Disable LangSmith tracing and private excerpt capture explicitly. Do not read,
  rewrite, or publish the existing private artifact in this step.
- Execute the existing M13 vertical canary only once after network approval.
  It is not the persisted LangGraph/UI workflow or a relevance benchmark.
- Record only safe stage outcomes, error categories, and aggregate counts/timing.
  Tokens and cost remain unknown unless measured; do not infer them.
- Stop at the observed result. No automatic live reruns, speculative code fixes,
  lowered thresholds, new provider integrations, Mem0 calls, durable shortlist
  writes, outreach, or trace uploads.
- Update README, runbook, triage, and build journal with the actual result and
  next bounded action. Recheck documentation contracts and formatting/lint/types.
