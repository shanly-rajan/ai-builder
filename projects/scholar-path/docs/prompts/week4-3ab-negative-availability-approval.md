# Week 4 step 3ab — record negative-availability behavior approval

## User response

> yes approved

## Context and scope

The response answers case 2, `draft-evidence-confirmed-not-accepting`: when identity,
affiliation, and research are verified but the profile explicitly states that new
doctoral students are not accepted, retain the verified facts, record
`confirmed_not_accepting` for that degree scope, and show `verified_with_concerns`
with a clear warning.

Record decision 002 in the existing human-review ledger, with the exact response,
date, reviewer role, and unchanged draft version/digest. Preserve decision 001 and
all earlier work. Update the progress summary to two intended-behavior approvals
and 28 cases awaiting review.

No approval is inferred for other cases, automatic Supervisor rejection/shortlisting,
broader degree availability, implementation changes, release, dataset freeze/upload,
live calls, commits, or pushes. The original manifest remains a pre-review snapshot;
the current scoped approvals are recorded separately in the ledger.
