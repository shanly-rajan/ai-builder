# Week 4 step 3r: one live private excerpt capture and offline replay

## User prompt

> lets move to next step

## Bounded interpretation

- Continue after step 3q by observing one explicitly opted-in, network-approved
  live canary with the prepared private excerpt capture enabled.
- Preserve the current worktree and checkpoint. No automatic commit or push.
- Keep the synthetic Candidate, public target, models, strict evidence gates,
  timeout clamps, provider call ceilings, and trace suppression unchanged.
- Store at most one eligible rejected affiliation excerpt and one rejected
  research excerpt in the existing ignored private artifact directory. Never
  retain full pages, Candidate content, claim prose, or full model responses.
- Replay any captured file offline. Inspect only the minimal matcher inputs
  locally; record counts, fixed reasons, and conclusions in public documentation,
  not exact names, URLs, excerpts, or matcher spans from the private capture.
- Do not automatically rerun if the canary fails or capture is empty. No matcher
  change, relaxed verification, new provider integration, Mem0 call, durable
  shortlist write, outreach, or LangSmith upload in this step.
- Validate current non-live tests before execution; save exact commands, results,
  assumptions, limitations, and the next bounded action in the build journal.
