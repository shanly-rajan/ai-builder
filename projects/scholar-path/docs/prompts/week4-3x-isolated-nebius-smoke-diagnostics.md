# Week 4 step 3x: isolated Nebius smoke diagnostics (offline)

## User prompt

> lets move tonext

## Bounded interpretation

- Continue step 3w's recommended offline preparation: make the existing isolated
  Nebius smoke test diagnosable without variable upstream search or extraction.
- Preserve the existing uncommitted work. No automatic commit or push.
- Keep fixed synthetic Candidate, Supervisor evidence, and Research Fit fixtures.
  Keep the current direct adapter invocation and response/reconciliation checks;
  do not substitute graceful degradation for a passing independent review.
- Add fixed stage/failure codes and aggregate counts only. Never serialize input
  records, evidence identifiers, URLs, model output, or exception text in summaries.
- Force tracing off around construction and invocation, clamp the per-request
  timeout to at most 60 seconds, and prevent a second logical model invocation.
- Use fakes to test successful/rejected output, invocation/validation failures,
  evidence-reference checks, reconciliation, exception propagation, privacy,
  tracing restoration, opt-in guards, and unchanged fixture inputs/check ordering.
- No live calls, credential/private-artifact inspection or edits, production-code
  changes, prompt/model tuning, weakened evidence/approval gates, or integrations.
- Run formatting, linting, types, and the complete non-live suite. Update README,
  runbook, triage, and build journal with exact results and limitations.
- Stop after offline validation. Any single isolated live Nebius observation is
  a separately approved next step, not a full-pipeline success or quality benchmark.
