# Week 4 step 3ad — record unsupported-availability behavior approval

## User response

> approve

## Context and scope

The response answers case 4, `draft-evidence-availability-without-statement`: the
model says a Supervisor is accepting students, but the source never states this.
Reject the unsupported assertion and keep availability as `not_stated`, not
"not accepting." Verification may still succeed if identity, affiliation, and
research evidence meet requirements.

Record decision 004 in the human-review ledger with the exact response, date,
reviewer role, and unchanged draft version/digest. Preserve decisions 001–003 and
all existing work. Update current progress to four intended-behavior approvals
and 26 cases awaiting review.

This is documentation-only review recording. Do not change source, schemas,
fixtures, or the immutable pre-review manifest; approve other cases; infer live
facts; freeze/upload a dataset; run live calls; commit; or push.
