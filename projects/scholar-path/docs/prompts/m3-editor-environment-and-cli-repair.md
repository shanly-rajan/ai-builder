# ScholarPath M3 repair: editor diagnostics, environment, and CLI testing

> the project scholarpath is red, see what errors need to be fixed, additionally
> create the .env file where I need to place the necessary secrets for this task as
> well as what should i run from a CLI to test this

Repair only the confirmed ScholarPath editor diagnostics. Create a local ignored
`.env` with blank secret placeholders, document offline and explicitly opted-in live
CLI commands, run the existing quality gates, archive this prompt, update the build
journal, and commit the repair separately. Do not add another product milestone or
make any provider request.
